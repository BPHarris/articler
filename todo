[TODO : DOING]

Check caption regex
Check url regex
figure data regex

add comments to grammar

better errors in new parser functions

-------------------------------------------------------------------------------

Move articler/core/ to articler-core repository and submodule
    Move style/themes/ to articler/core/themes/
Add articler to projects/websites on website

Https redirect

Create default articler theme

Host on articler.bpharris.uk
Hide common/, php/, on articler.bpharris.uk

-------------------------------------------------------------------------------

Parse b, i, u, a inside para ===> proper text rule grammar/parser

escape html
parse_html
parser options

figure_data

line numbers in errors

-------------------------------------------------------------------------------

set CodeMirror language
fix indent to tabs
fix spellcheck, autocorrect, autocapitalize
set scrollbarStyle, screenReaderLabel,
fix text selection after changing font size

working download buttons
    change to copy-to-clipboard buttons?
make ctrl+s download

fill in readme

parser unittests


[TODO : UP NEXT]

auto table of contents
    generate numeric id for each {title, subtitle, subsubtitle, figure}
    
    other id = title level - relative subtitle level - relative subsubtitle level - relative figure number
        e.g.   t1-s1-s2-f1 is first figure in the 2 subsub of first sub of the first (usually only) title
    
    toc format
        foreach id:
            <a href="#id">indent + id number + title/subtitle/subsubtitle/figure</a>
auto table of contents should take a level
    \autotoc 1 => table of conents with title and all headings
    \autotoc 2 => table of conents with title and all headings/subheadings
        ...    => ...
    \autotoc 6 => table of conents with title and all h1-h6
manual toc (\toc)
    only include headings with the \toc

make it look nice


[TODO : BACKBURNER]

allow commands in text
allow user to register commands?

media query width small =>
    decreade control-bar padding
    only show editor/viewer one at a time

    add editor control-bar button to switch to, and refresh, viewer
        hide refresh-viewer button
    add viewer control-bar button to switch to editor
media query height small =>
    hide footer
    decrease header padding
